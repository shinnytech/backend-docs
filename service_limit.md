# 输入报文约束及 Best Practice

## subscribe_quote

- `ins_list` 合约数量不应超过 2000，大于 2000 应当拆分多连接

## set_chart

- `ins_list` 合约数量不应超过 20
- 同一个连接下订阅的 chart 数量不得超过 10000

## ins_query

- 每一个 `ins_query` 查询结束、客户端收到对应回包后，都应发送退订报文
- 对于 `ins_query` 合约信息查询，应当使用少量、多次的取数据方式，每次只获取必要的数据字段。应当避免大规模查询（如查询全市场全字段），以减少冗余信息传输、减少服务端/客户端负载、降低计算和通讯耗时。

## set_combo_chart

- `weights` 合约数量不应超过 20
- 同一个连接下订阅的 chart 数量不应超过 20